# Рекомендательная система (Matrix Factorization, SGD)

## 📌 Описание проекта

Проект реализует рекомендательную систему для прогнозирования пользовательских оценок фильмов на основе **матричной факторизации**, обучаемой методом **стохастического градиентного спуска (SGD)** с использованием **валидации и early stopping**.

В качестве датасета используется MovieLens (ratings.dat, movies.dat).

Проект последовательно реализует:
1. Загрузку и анализ датасета
2. Разбиение на train / test и baseline-модели
3. Обучение модели матричной факторизации
4. Оценку качества (RMSE) и сохранение результатов

---

## 📂 Структура проекта

```
HW_var22/
├── CMakeLists.txt
├── include/
│   ├── rating.h
│   ├── dataset.h
│   └── mf_model.h
├── src/
│   ├── main.cpp
│   ├── dataset.cpp
│   └── mf_model.cpp
├── data/
│   ├── ratings.dat
│   └── movies.dat
├── results/
│   ├── dataset_stats.csv
│   ├── baseline_rmse.csv
│   ├── user_rating_counts.csv
│   ├── movie_rating_counts.csv
│   └── mf_training_log.csv
└── README.md
```

---

## 📄 Назначение файлов

### 🔧 Конфигурация сборки

### 📁 include/

- **rating.h**
  Описание структуры рейтинга пользователя.

- **dataset.h**
  Загрузка датасета, подсчёт статистики и сохранение CSV.

- **mf_model.h**
  Описание параметров и класса модели матричной факторизации.

---

### 📁 src/

- **main.cpp**
  Основной сценарий выполнения: загрузка данных, обучение, оценка.

- **dataset.cpp**
  Реализация функций работы с данными.

- **mf_model.cpp**
  Реализация SGD, early stopping и RMSE.

---

## ⚙️ Основные функции

- `train_test_split(...)`
- baseline RMSE (глобальное и пользовательское)
- `MFModel::fit_with_early_stopping(...)`
- `MFModel::evaluate(...)`
- `MFModel::save_training_log_csv(...)`

---

## ▶️ Сборка и запуск

```bash
rm -rf build && cmake -B build && cmake --build build && ./build/app
```

---

## 📁 Результаты

Все выходные данные сохраняются в папке `results/`.
